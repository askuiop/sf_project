{% extends '::base.html.twig' %}

{% block body -%}
    <h1>StUser creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('stuser') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
