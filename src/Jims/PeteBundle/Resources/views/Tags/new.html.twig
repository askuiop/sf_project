{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Tags creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('tags') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
